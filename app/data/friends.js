var friends = [{
    name: name,
    photo: photo,
    scores: []
}];

scores = [];

    for(i = 0; i < scores.length; i++) {
        var difference = Math.abs(scores1[i] - scores2[i]);
        for (var i = 0, sum = 0; i < difference.length; sum += difference[i++]);
